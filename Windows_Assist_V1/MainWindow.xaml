<Window x:Class="Windows_Assist_V1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Windows_Assist_V1"
        mc:Ignorable="d"
        Title="PowerShell Assistant" 
        Height="220" Width="520"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent" 
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Converter for TextBox placeholder -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Modern button style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#40FFFFFF"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#60FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#26FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#E0FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="24"
                                BorderThickness="1"
                                BorderBrush="#20FFFFFF">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                             Padding="{TemplateBinding Padding}"
                                             VerticalAlignment="Center"
                                             Margin="0,0,30,0"/>
                                <TextBlock x:Name="PlaceholderText"
                                           Text="Enter your request..."
                                           Foreground="#80FFFFFF"
                                           Margin="18,0,0,0"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding Text.IsEmpty, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVis}}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30FFFFFF"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="#38FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Spinner animation -->
        <Style x:Key="SpinnerStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Grid>
                            <Grid.RenderTransform>
                                <RotateTransform x:Name="Rotation" CenterX="10" CenterY="10" />
                            </Grid.RenderTransform>
                            <Ellipse Width="20" Height="20" Stroke="White" StrokeThickness="2" 
                                     Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse.StrokeDashArray>
                                    <DoubleCollection>
                                        <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib">2</sys:Double>
                                        <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib">2</sys:Double>
                                    </DoubleCollection>
                                </Ellipse.StrokeDashArray>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         To="360" Duration="0:0:1" RepeatBehavior="Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" 
            Background="#DA121212" 
            CornerRadius="28" 
            BorderThickness="1" 
            BorderBrush="#2AFFFFFF">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.6" Color="#222222"/>
        </Border.Effect>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="#4C9BE8" Margin="0,0,8,0"/>
                    <TextBlock Text="PowerShell Assistant" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               Foreground="White" 
                               Margin="0,0,0,16"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button x:Name="MinimizeButton" 
                            Style="{StaticResource ModernButton}" 
                            Click="MinimizeButton_Click"
                            Margin="0,0,6,0"
                            ToolTip="Minimize">
                        <Rectangle Width="12" Height="2" Fill="White" />
                    </Button>
                    <Button x:Name="CloseButton" 
                            Style="{StaticResource ModernButton}" 
                            Click="CloseButton_Click"
                            ToolTip="Close">
                        <Path Data="M 0,0 L 12,12 M 12,0 L 0,12" 
                              Stroke="White" 
                              StrokeThickness="1.5" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Input Box -->
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <TextBox x:Name="UserInput" 
                         Style="{StaticResource ModernTextBox}" 
                         KeyDown="UserInput_KeyDown" 
                         Height="50"/>

                <Control x:Name="ProcessingSpinner" 
                        Style="{StaticResource SpinnerStyle}" 
                        Width="20" Height="20" 
                        HorizontalAlignment="Right" 
                        Margin="0,0,22,0"
                        Visibility="Collapsed"/>

            </Grid>
            <!-- Status Text -->
            <StackPanel Grid.Row="2" 
                       Orientation="Horizontal"
                       Margin="8,12,0,0">
                <Ellipse Width="6" Height="6" Fill="#4CCA64" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusText" 
                       Text="Ready" 
                       Foreground="#AAAAAA"
                       FontSize="12"
                       FontFamily="Segoe UI"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>