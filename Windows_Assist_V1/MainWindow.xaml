<Window x:Class="Windows_Assist_V1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Windows_Assist_V1"
        mc:Ignorable="d"
        Title="PowerShell Assistant" 
        Height="200" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent" 
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Custom button style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#40FFFFFF"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#60FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#20FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                         Padding="{TemplateBinding Padding}"
                                         VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30FFFFFF"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="#40FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Spinner animation -->
        <Style x:Key="SpinnerStyle" TargetType="Control">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Grid>
                            <Grid.RenderTransform>
                                <RotateTransform x:Name="Rotation" CenterX="10" CenterY="10" />
                            </Grid.RenderTransform>
                            <Ellipse Width="20" Height="20" Stroke="White" StrokeThickness="2" 
                                     Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse.StrokeDashArray>
                                    <DoubleCollection>
                                        <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib">2</sys:Double>
                                        <sys:Double xmlns:sys="clr-namespace:System;assembly=mscorlib">2</sys:Double>
                                    </DoubleCollection>
                                </Ellipse.StrokeDashArray>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         To="360" Duration="0:0:1" RepeatBehavior="Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" 
            Background="#CC1E1E1E" 
            CornerRadius="15" 
            BorderThickness="1" 
            BorderBrush="#30FFFFFF">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <TextBlock Text="PowerShell Assistant" 
                           FontSize="18" 
                           FontWeight="Medium" 
                           Foreground="White" 
                           Margin="0,0,0,15"
                           VerticalAlignment="Center"/>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button x:Name="MinimizeButton" 
                            Style="{StaticResource ModernButton}" 
                            Click="MinimizeButton_Click"
                            Margin="0,0,5,0"
                            Content="_"
                            FontWeight="Bold"/>
                    <Button x:Name="CloseButton" 
                            Style="{StaticResource ModernButton}" 
                            Click="CloseButton_Click"
                            Content="×"
                            FontSize="16"
                            FontWeight="Bold"/>
                </StackPanel>
            </Grid>

            <!-- Input Box -->
            <Grid Grid.Row="1">
                <TextBox x:Name="UserInput" 
                         Style="{StaticResource ModernTextBox}" 
                         KeyDown="UserInput_KeyDown" 
                         Height="45" 
                         Margin="0,0,0,0"
                         VerticalContentAlignment="Center" 
                         FontFamily="Segoe UI"
                         CaretBrush="White"
                         CharacterCasing="Normal"/>

                <TextBlock Text="Enter your request..." 
                           Foreground="#80FFFFFF" 
                           Margin="20,0,0,0" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=UserInput}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Text, ElementName=UserInput}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Text, ElementName=UserInput}" Value=""/>
                                        <Condition Binding="{Binding IsFocused, ElementName=UserInput}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </MultiDataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Text, ElementName=UserInput, Converter={StaticResource IsNotEmptyConverter}}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Hidden"/>
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Control x:Name="ProcessingSpinner" 
                        Style="{StaticResource SpinnerStyle}" 
                        Width="20" Height="20" 
                        HorizontalAlignment="Right" 
                        Margin="0,0,20,0"
                        Visibility="Collapsed"/>
            </Grid>

            <!-- Status Text -->
            <TextBlock x:Name="StatusText" 
                       Grid.Row="2" 
                       Text="Ready" 
                       Margin="15,10,0,0"
                       Foreground="Gray"
                       FontSize="12"
                       FontFamily="Segoe UI Light"/>
        </Grid>
    </Border>
</Window>